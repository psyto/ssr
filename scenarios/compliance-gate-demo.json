{
  "name": "compliance-gate-demo",
  "category": "stress",
  "description": "Self-contained, no-validator demo of the compliance gate. Five synthetic participants (one each of VERIFIED / PENDING / SUSPENDED / BLOCKED, plus a second VERIFIED counterparty) are evaluated against `AccountRecord::check_transfer_allowed` for every sender-receiver pair. The output is a 5×5 matrix of allow / reject decisions plus a named reason for each reject (`sender SUSPENDED`, `receiver BLOCKED`, …). Useful for asking: which statuses actually clear the gate? What error does an operator see for a specific reject? How does the gate's behavior change if a verified counterparty is suspended mid-flow?\n\nRuns in milliseconds against pure-Rust `ssr-types` primitives — no Solana validator, no LiteSVM, no deployed program required.",
  "headline": "Five participants × 5 = 25 pairs; only the two VERIFIED ↔ VERIFIED transfers clear the gate, each reject carries a named reason.",
  "steps": [
    {
      "explanation": "Run the in-process compliance gate matrix demo. Builds a synthetic participant population and exhaustively evaluates every (sender, receiver) pair.",
      "command": "ssr-cli compliance-gate-demo",
      "expect": "VERIFIED"
    }
  ],
  "expected_outcomes": [
    {
      "name": "two-allowed",
      "description": "Exactly 2 pairs clear the gate (alice↔eve, the only VERIFIED↔VERIFIED combinations)",
      "check": {
        "allowed_pairs": 2
      }
    },
    {
      "name": "eighteen-rejected",
      "description": "Exactly 18 pairs are rejected (every non-VERIFIED↔VERIFIED combination)",
      "check": {
        "rejected_pairs": 18
      }
    },
    {
      "name": "alice-to-eve-allowed",
      "description": "alice (VERIFIED) → eve (VERIFIED) clears the gate",
      "check": {
        "allowed_pair": {
          "sender": "alice",
          "receiver": "eve"
        }
      }
    },
    {
      "name": "eve-to-alice-allowed",
      "description": "eve (VERIFIED) → alice (VERIFIED) clears the gate (symmetric)",
      "check": {
        "allowed_pair": {
          "sender": "eve",
          "receiver": "alice"
        }
      }
    },
    {
      "name": "bob-rejected-as-sender",
      "description": "bob (SUSPENDED) sending to alice is rejected with reason naming SUSPENDED",
      "check": {
        "reject_reason": {
          "sender": "bob",
          "receiver": "alice",
          "reason_contains": "SUSPENDED"
        }
      }
    },
    {
      "name": "dan-rejected-as-receiver",
      "description": "alice sending to dan (BLOCKED) is rejected with reason naming BLOCKED",
      "check": {
        "reject_reason": {
          "sender": "alice",
          "receiver": "dan",
          "reason_contains": "BLOCKED"
        }
      }
    }
  ]
}
